#include "Develop/Projects/SR2/pgm/src/Data/Path/PathCom.hpp"

void clsPathCom::serarchNextAILine(clsRetPath* pcRetIO, s32 s32PointNo)
{
}

u8 clsPathCom::searchCourseAIPath(clsRetPath* pcRetIO,
                                  const NNS_VECTORFAST* psPosition,
                                  u32 u32DisableRoots,
                                  bool bNextSearchFlag)
{
    return 0;
}

NNS_VECTORFAST* clsPathCom::searchGCtrlEndPoint(const _LineData* psStartLine)
{
    return 0;
}

NNS_VECTORFAST* clsPathCom::searchTrickGCtrlConnectPoint(const _LineData* psStartLine,
                                                         u32 u32Ability)
{
    return 0;
}
