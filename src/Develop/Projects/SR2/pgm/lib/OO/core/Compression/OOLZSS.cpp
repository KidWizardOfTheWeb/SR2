#include "Develop/Projects/SR2/pgm/lib/OO/core/Compression/OOLZSS.hpp"

s32 clsOOLZSS::done(clsOOBuffer& rcDstBuf,
                    clsOOBuffer& rcSrcBuf,
                    bool bIsComp,
                    const volatile u32& rvu32IsAbort)
{
    return 0;
}

s32 clsOOLZSS::done(clsOOBuffer& rcDstBuf,
                    clsOORingBuffer& rcSrcBuf,
                    bool bIsComp,
                    const volatile u32& rvu32IsAbort)
{
    return 0;
}

s32 clsOOLZSS::done(clsOORingBuffer& rcDstBuf,
                    clsOOBuffer& rcSrcBuf,
                    bool bIsComp,
                    const volatile u32& rvu32IsAbort)
{
    return 0;
}

s32 clsOOLZSS::done(clsOORingBuffer& rcDstBuf,
                    clsOORingBuffer& rcSrcBuf,
                    bool bIsComp,
                    const volatile u32& rvu32IsAbort)
{
    return 0;
}
